id: PLAN-020
title: "HELIX 最新基準適合化整備 (gpt-5.5 追従 / 委譲 Codex コミット禁止 / プロジェクト CLAUDE.md・AGENTS.md 強化 / PLAN.yaml D-shard auto-skeleton + G2 枚数 gate / PLAN-019 helix-migrate 実装)"
status: finalized
created_at: 2026-05-05T17:10:00Z
finalized_at: 2026-05-05T17:42:00Z
source_file: null
note: |
  ユーザー方針: 「ヘリックス開発は最新で入らないと意味ない、計画の内容はどうでもいい、5.5 は CLAUDE.md に忘れ、真実は実装、並列で」
  → PLAN draft の review round trip は省略し、実装ファースト。最新 framework の整合のみ確保し、過去 PLAN の D-shard 復元は行わない。
sprints:
  - id: ".1"
    name: "gpt-5.5 docs 追従 + プロジェクト CLAUDE.md/AGENTS.md に §Agent tool コスト制御 / モデル割当 移植"
    owner: opus
    files:
      - "/home/tenni/.claude/CLAUDE.md"
      - skills/SKILL_MAP.md
      - helix/HELIX_CORE.md
      - helix/CODEX_TL_MODE.md
      - CLAUDE.md
      - AGENTS.md
  - id: ".2"
    name: "cli/helix-codex CODEX_DISCIPLINE_PROMPT に委譲 Codex 限定コミット禁止追加 + memory feedback 注記"
    owner: opus
    files:
      - cli/helix-codex
  - id: ".3"
    name: "PLAN.yaml schema 拡張 (references/artifacts) + helix plan draft で D-shard skeleton 自動生成 + helix gate G2 に最低枚数チェック追加"
    owner: codex_se
    files:
      - cli/helix-plan
      - cli/lib/plan_*.py
      - cli/helix-gate
      - cli/templates/gate-checks.yaml
      - cli/lib/gate_check_generator.py
  - id: ".4"
    name: "PLAN-019 実装 (helix-migrate target registry 化 + CLAUDE.md/AGENTS.md/.claude/settings.json 対象拡張 + json_hooks merge_strategy)"
    owner: codex_se
    files:
      - cli/lib/migrate.py
      - cli/helix-migrate
      - cli/lib/merge_settings.py
      - cli/tests/
parallel_dispatch:
  rationale: "Sprint .1 (docs) / .2 (cli/helix-codex) / .3 (helix-plan + helix-gate) / .4 (cli/lib/migrate.py + merge_settings.py) は編集ファイルが独立、依存なし、共有状態同時更新なし。Opus 直接 (.1/.2) と Codex 委譲 (.3/.4) を同時並走"
results:
  sprint_1: "completed (Opus 直接 Edit)"
  sprint_2: "completed (Opus 直接 Edit + memory feedback 注記)"
  sprint_3: "completed (Codex SE bs3810qsd, 15 pytest + 10 bats PASS)"
  sprint_4: "completed (Codex SE bsxabrv92, 23 pytest PASS、最終 verdict 出力前に CPU 0% で 21 分 hang したため Opus が kill。ファイル編集は完了済みで pytest 検証で代替)"
followups:
  - "完了後 PLAN-021 として配布物分離 (dogfood/.helix/ 物理移動 = A 案) を切る"
  - "PLAN-019 P2 finding 3 件は Sprint .4 で全消化済み"
  - "feedback_codex_hang_and_idle 教訓再確認: CPU 0% 5 分超 = kill ルールが今回も有効"
review:
  status: skipped_by_user_directive
  note: "ユーザー指示『計画の内容はどうでもいい、真実は実装、並列で』により draft round trip 省略"
