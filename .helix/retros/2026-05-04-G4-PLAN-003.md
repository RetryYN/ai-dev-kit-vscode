# Mini Retro: G4 (2026-05-04) — PLAN-003 auto-restart Foundation

## Keep
- PLAN-002 から HMAC/HOME DB/CURRENT v2 を分離し、複雑な継続制御の責務を独立できた。
- B/D outcome と scope_hash/rebaseline の考え方を PLAN 単位で追跡可能にした。
- readiness retro を PLAN-004 v5 と接続した。

## Problem
- 当時は HMAC canonical 入力、CURRENT v2 normalized boundary、rate limit key が P2 deferred として残っていた。
- 文書版数が旧版のままで、改訂履歴と不一致だった。
- security boundary に近い項目が多く、実装前に人間承認の要否を切り分ける必要がある。

## Try
- @codex 2026-05-11 以下の改善を次トラックで扱う（2026-05-04 に文書契約へ反映済み）。
- HMAC canonical input は schema table と fixture で固定する。
- rate limit は `workspace_hash + task_id` などの候補を PM/TL で承認してから実装する。
- CURRENT v2 は raw/normalized/hash target の順序を D-CONTRACT として凍結する。

## Evidence
- Plan: `docs/plans/PLAN-003-auto-restart-foundation.md`
- Deferred: `.helix/audit/deferred-findings.yaml`

## Resolution Update (2026-05-04)
- HMAC canonical input, CURRENT v2 normalized boundary, and rate limit key were reflected into the PLAN document.
- `./cli/helix readiness list --status open,carried` now returns `no findings`.
