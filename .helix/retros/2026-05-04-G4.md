# Mini Retro: G4 (2026-05-04)

## Phase: 実装フェーズ (L3-L4)

## Keep
- PLAN-013 の DoD 残を実装証跡で再検証し、未同期チェックを 0 にできた。
- `private_helper` の seed metadata 契約をテストで固定し、PLAN-011 seed 5件だけを明示 seed として保持できた。
- `helix task plan/status` の f-string SyntaxError を修正し、工程表コマンドを復旧できた。

## Problem
- PLAN-013 は実装済みでも PLAN 本文の checkbox が古く、roadmap 上で部分実装扱いになっていた。
- `helix task plan/status` が stderr を隠して無言失敗しており、工程表生成の不具合検出が遅れた。
- G4 static の `src/` 向け advisory は CLI-only リポジトリでは warning になりやすい。

## Try
- @codex 2026-05-05 CLI-only project 向けに G4 static advisory の対象ディレクトリ検出を改善する。
- @codex 2026-05-04 PLAN-002〜010 の retro 未作成を completion roadmap の次トラックで解消済み。

### メモ
- Verification: `python3 -m pytest cli/lib/tests/ -q --tb=short` -> 696 passed.
- Verification: `./cli/helix test --no-pytest --bats-only` -> 237 Bats passed + 5 shell checks passed.
- Gate: `./cli/helix gate G4` -> PASS, unresolved debt 0.
