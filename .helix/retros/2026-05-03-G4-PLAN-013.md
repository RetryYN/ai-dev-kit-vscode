# Mini Retro: G4 (2026-05-03) — PLAN-013 Code Index Eligibility Taxonomy

## Phase: 実装フェーズ (L3-L4 / Sprint .1b〜.5)

PLAN-013 v1.4 finalize → v1.5 schema freeze → Sprint .2-.5 全完走 (1 セッション内、6 commits: b12cc10..6b14b0a)。pytest 655 / bats 191 / shell 572 全 PASS、core5 coverage 45/56 = 80.4% を維持しつつ 3-bucket taxonomy (coverage_eligible=417 / private_helper=571 / excluded=29) を導入。

### Keep（うまくいったこと）
- v1.5 schema freeze で F1/F2/F3 の判断を方針 A 統一に揃え、意思決定速度を最大化した
- helix.db v14→v15 マイグレーション (`bucket` / `symbol_line` 追加) を Sprint .2 単一の DB change に集約、後続スプリントの DB 競合を回避
- Sprint .3 は CLI flag (`--bucket` / `--seed-candidate` / `--seed-promotable`) と flat output 契約に役割を絞り、scope が線形に進んだ
- Sprint .4 で DoD 未カバー領域の単体テストを後付けで埋めた（テスト記述を実装後にまとめたことで設計の理解度が高い状態で網羅できた）
- Codex SE/QA/docs ロール分担で並列投入し、機械的編集を Codex に、判断と finalize を Opus に分けた
- core5 80% gate は taxonomy 拡張中もクリアし続けた（gate 退行ゼロ）

### Problem（問題があったこと）
- v1.3 の段階で `--scope` default を独断で `core5` と書き換え、現行 CLI の挙動を grep で確認しないまま PLAN を更新 → TL レビューで 1 ラウンド regress した（feedback_plan_revision_grep_first.md として memory 化済）
- 後半の細かい PLAN 修正で docs ロール round-trip を続けたため、200 行の修正指示書を書く時間が Opus 直接 Edit 30 行より高くついた（feedback_docs_role_vs_opus_edit.md として memory 化済）
- `.helix/session-summaries/2026-05-03-session.md` に Stop hook 由来の重複行 58 件が積み上がった。手動圧縮はその場しのぎで根本対処にならない（hook 側の idempotency 不足）

### Try（次に試すこと）
| アクション | 担当 | 期日 |
|-----------|------|------|
| PLAN 修正前に `rg`/`Read` で現行実装を必ず確認する運用を default 化（feedback memory を session-start で再読込） | Opus | 次セッション開始時 |
| 3〜5 件の small Edit は docs ロール経由ではなく Opus 直接 Edit を default にする | Opus | 次セッション開始時 |
| Stop hook の重複追記抑制（同日内のセッション終了は 1 ブロックに集約 / 別ファイル分離）を PLAN-014 候補として起票 | Opus → docs | 次セッションで draft |
| deferred-findings 18 件 P2 の burn-down 計画を debt-register で追跡 | Opus | PLAN-014 と同時に検討 |
| seed_promotable heuristic（現状は `_` 始まりかつ複数参照のヒューリスティック）の精度評価とパラメタ可変化検討 | TL → Opus | PLAN-014 起票時 |

### メモ
- 6 commits 内訳: b12cc10 (PLAN finalize) / 24b4e44 (v1.5 schema freeze) / bcf1519 (Sprint .2 v15+classifier) / e0394d9 (Sprint .3 CLI flag) / 869de67 (Sprint .4 tests) / 6b14b0a (Sprint .5 SKILL_MAP/HELIX_CORE 反映)
- Sprint 3-bucket union: coverage_eligible=417 / private_helper=571 / excluded=29、non_indexable_paths は bucket 分類前 pre-filter として実装
- G4 coverage gate: `helix code stats --scope core5 --bucket coverage_eligible --fail-under 80` → 45/56 = 80.4% PASS
- PLAN-013 はコードインデックス系 PLAN-011 (catalog skeleton) → PLAN-012 (coverage gate) → PLAN-013 (taxonomy) の三段重ねの最終層。これで「公開資産は coverage 母集団に必ず入る／private helper は seed 候補に分離／除外は明示」の 3 層分類が確定
