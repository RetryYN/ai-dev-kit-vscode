# Whole-source coverage 機能設計（FN-WSC-* / zero-omission B' Action4）

> coverage_layer=`L6_required` の全 module/hook の関数設計（DbC: requires/ensures/invariant）。
> 正本接続: L3 `helix-workflows-functional-registry.md` §1.6 / verification-strategy §11.7。
> Process: `process-2026-06-07-whole-source-design-coverage-closure` Action4。
> 各 FN-WSC-* は L7 `whole-source-coverage-単体テスト設計.md` の UT-WSC-* と 1:1（trace_symmetry L6↔L7）。
> DbC は pmo-project-explorer が**実コード精読**で抽出（fail-close 方向は実コード確認済）。

## §A. Hooks (.claude/hooks/*, FN-WSC-01〜17)

| ID | hook名 | イベント | 対象 | requires | ensures | invariant / fail-close | 既存テスト | UT観点 |
|----|--------|---------|------|----------|---------|----------------------|-----------|--------|
| FN-WSC-01 | post-tool-use.sh | PostToolUse | 全ツール | helix-common.sh がロード可能、helix_db.py が import 可能、DB パスが解決できる | hook_exec の audit ログを DB に insert する | fail-open: DB エラーや python 失敗は `|| true` で握り潰し、exit 0 で続行する | 部分あり (test_audit_log.py が insert_audit_log をカバー) | (1) DB 書き込み成功 → audit_log に hook_exec が1件 insert される (2) DB パス解決失敗 → 例外を握り潰して exit 0 (3) HELIX_AUTOMATION_RUN_ID が空 → run_id=None で insert される |
| FN-WSC-02 | posttooluse-design-doc-web-search-revert.sh | PostToolUse | Edit / Write / MultiEdit | stdin に tool_name / tool_input が含まれる JSON、PROJECT_ROOT が存在する | 対象外ツール→pass(exit 0)、対象外パス→pass(exit 0)、WebSearch 証跡あり→pass(exit 0)、証跡なし かつ frontmatter あり かつ 100 行以上→ファイルを HEAD に revert して block(exit 2)、revert 条件未達→warn(exit 1) | fail-close: 証跡なし+大規模設計 doc は revert+block。session_id 取得不能は warn(exit 1) で hold、bypass は HELIX_ALLOW_DESIGN_DOC_NO_WEB=1+reason 必須 | あり (test_design_doc_web_search_guard.bats、test_helix_gate_design_doc_fail_close.py が pre-side をカバー。revert side は test_security_hardening.py で一部カバー) | (1) 証跡なし+frontmatter+100行以上 → revert が実行されバックアップが .helix/backups/ に生成される (2) 証跡あり → revert は行われず pass (3) HELIX_ALLOW_DESIGN_DOC_NO_WEB=1 かつ reason 未設定 → warn(exit 1) (4) 行数 <100 → revert されず warn(exit 1) (5) docs/plans/ パス → 対象外でスキップ |
| FN-WSC-03 | posttooluse-helix-job-enqueue.sh | PostToolUse | Write / Edit / MultiEdit | stdin JSON に tool_name / tool_input が含まれる | PLAN-*.md / ADR-*.md の変更を検出した場合に systemMessage で候補提示する（enqueue は行わない）。consent_required かつ authorized_by=none → advisory のみ。authorized_by 確定時 → 承認根拠付き candidate 通知。常に decision:continue を返す | fail-open: python 失敗は `|| true`、スクリプト末尾は `exit 0` 固定。enqueue の実行は行わず advisory only | 未確認（test_plan_parser.py / test_plan_registry.py は PLAN parser 側のカバー、hook script 自体の直接テストなし） | (1) PLAN-*.md を Edit → systemMessage に plan token が含まれる (2) HELIX_JOB_EXPLICIT_CONSENT=1 → authorized_by=explicit_consent を返す (3) consent_required かつ全フラグ未設定 → authorized_by=none で advisory メッセージが出る (4) 対象外ツール → exit 0 で何も出力しない |
| FN-WSC-04 | posttooluse-plan-auto-register.sh | PostToolUse | Write / Edit / MultiEdit | stdin JSON に tool_result / tool_response / tool_input のいずれかに file_path が含まれる、python3 が利用可能、plan_parser.py が REPO_ROOT/cli/lib/ に存在する | docs/plans/PLAN-*.md または docs/adr/ADR-*.md の保存時に plan_parser --mode upsert を実行し plan_registry に upsert する。循環依存を検出した場合は decision:block(exit 2)を返す。成功時は decision:continue + systemMessage | fail-close (循環依存のみ): cycle 検出時のみ exit 2/block。その他のエラー（parse 失敗 / DB エラー / python 不在）は decision:continue + WARNING で fail-open | あり (test_plan_parser.py が parser/upsert をカバー、test_plan_registry.py が registry をカバー。hook script の E2E は未確認) | (1) docs/plans/PLAN-001-xxx.md を保存 → plan_registry に upsert されステータスが反映される (2) 循環依存のある PLAN を保存 → decision:block が返される (3) python3 不在 → WARNING メッセージを出力して exit 0 (4) parse 失敗 → WARNING を出力して exit 0 |
| FN-WSC-05 | posttooluse-skill-catalog-rebuild.sh | PostToolUse | Edit / Write / MultiEdit | stdin JSON に tool_name / tool_input が含まれる | skills/<domain>/<name>/SKILL.md を変更した場合、debounce 確認後に CACHE_DIR の *.json を削除し `helix skill catalog rebuild` をバックグラウンドで起動する。debounce 内なら何もしない | fail-open: python 判定失敗 / rebuild 失敗 / cache 削除失敗 は `|| true` で続行。hook 自体は常に exit 0 | 部分あり (test_skill_catalog.py / test_skill_catalog_integration.py が catalog rebuild ロジックをカバー。hook script の直接テストなし) | (1) SKILL.md を Edit → should_trigger=1 でバックグラウンド rebuild が起動される (2) debounce 期間内の連続編集 → 2回目以降は rebuild をスキップ (3) SKILL.md 以外を Edit → should_trigger=0 で何も起動しない (4) 対象外ツール (Bash等) → exit 0 即時 |
| FN-WSC-06 | precompact-state-snapshot.sh | PreCompact | (PreCompact イベント) | stdin JSON に session_id が含まれる(またはenv) | persist_failed=true かつ unsaved_decisions=true かつ one_shot_consumed=false → decision:block。それ以外 → state を .helix/backups/precompact/ にバックアップして decision:continue | fail-open (python 失敗時): python3 失敗の場合 `{"decision":"continue","message":"PreCompact: fail-open fallback"}` で続行。block 条件は 3 条件 AND が必要 | 未確認（test_security_hardening.py で断片カバーの可能性のみ、専用テストなし） | (1) persist_failed=true + unsaved_decisions=true + one_shot_consumed=false → decision:block が返され blocked_sessions_file に session_id が追記される (2) 同 session_id で2回目 → one_shot_consumed=true となり decision:continue (3) python3 実行失敗 → fail-open で decision:continue (4) persist_failed=false → block されず continue |
| FN-WSC-07 | pretooluse-agent-fire.sh | PreToolUse | Agent | stdin JSON に tool_name=Agent と tool_input.subagent_type が含まれる | agent_slots.fire_slot() を呼び出し、subagent_type / session_id / slot_source を DB に記録する | fail-open: fire_slot 失敗 / timeout は `|| true` で握り潰し exit 0。session 阻害禁止が明示されている。subagent_type 未指定は skip して exit 0 | あり (tests/pretooluse-agent-fire.bats が E2E カバー、test_agent_slots_unit.py / test_agent_slots_integration.py が fire_slot をカバー) | (1) Agent ツール + 有効な subagent_type → agent_slots テーブルに fire 記録が挿入される (2) subagent_type が空 → fire_slot を呼ばずに exit 0 (3) Agent ツール以外 → exit 0 即時 (4) fire_slot が例外を投げる → exit 0 で続行 (セッション非阻害) |
| FN-WSC-08 | pretooluse-agent-guard.sh | PreToolUse | Agent | stdin JSON に tool_name / tool_input.subagent_type / tool_input.model が含まれる、.claude/agents/<name>.md が存在する | subagent_type が許可リスト(12種)内 かつ definition ファイルが存在 かつ model が frontmatter family と一致(または省略) → pass(exit 0)。違反時は exit 2(block) | fail-close: subagent_type 未指定→block、許可リスト外→block、definition 未存在→block、model family 不一致→block。HELIX_ALLOW_RAW_AGENT=1 のみ bypass 可、effort 未定義は warn のみ(block しない) | あり (tests/helix-agent-mandatory.bats / tests/helix-agent.bats が E2E カバー) | (1) 許可リスト外 subagent_type → exit 2 (2) subagent_type 未指定 → exit 2 (3) definition ファイル不在 → exit 2 (4) model が frontmatter family と不一致 → exit 2 (5) model 省略 → pass(exit 0) (6) HELIX_ALLOW_RAW_AGENT=1 で bypass → exit 0 + warn (7) effort 未定義 → warn のみ exit 0 |
| FN-WSC-09 | pretooluse-askuserquestion.sh | PreToolUse | AskUserQuestion | stdin JSON に tool_name が含まれる、helix_db.py が import 可能、invocation_log テーブルに tl-advisor の記録がある | AskUserQuestion ツール呼び出し時、直近5分以内に tl-advisor の invocation_log がある → pass(exit 0)。ない → deny + 詳細エラーメッセージ。DB 不在 / 例外 → warn(exit 0、deny しない) | fail-close: tl-advisor 相談なし → hookSpecificOutput.permissionDecision=deny。HELIX_ALLOW_ASKUSER=1 + HELIX_ASKUSER_REASON で bypass 可 | あり (test_pretooluse_askuserquestion.py が Python ロジックをカバー) | (1) tl-advisor が直近5分以内に記録あり → pass (2) 記録なし → deny + エラーメッセージ (3) DB 不在 → warn(pass) (4) HELIX_ALLOW_ASKUSER=1 + HELIX_ASKUSER_REASON設定 → systemMessage 付きで pass (5) HELIX_ALLOW_ASKUSER=1 かつ HELIX_ASKUSER_REASON 未設定 → warn メッセージ付きで pass (6) AskUserQuestion 以外のツール → pass |
| FN-WSC-10 | pretooluse-codex-slot-check.sh | PreToolUse | Bash | stdin JSON に tool_name=Bash かつ command に "helix codex" または "helix-codex" が含まれる | active slot 数が 6 以上の場合 system-reminder を stderr に出力し harness_monitor に slot_count_warning を記録する。--wbs-id 指定時に --reference-doc 未指定の場合も警告を記録する。常に exit 0 | fail-open: 全エラーは `|| true` / `2>/dev/null` で握り潰し exit 0。警告のみで block しない | 部分あり (test_harness_monitor_unit.py / test_harness_monitor_integration.py が record_event をカバー。hook script 自体の直接テストなし) | (1) active slot >= 6 → stderr に system-reminder が出力され harness_monitor に warning が記録される (2) active slot < 6 → 何も出力しない (3) --wbs-id あり --reference-doc なし → 追加警告が出る (4) コマンドに "helix codex" 非含む Bash → exit 0 即時 |
| FN-WSC-11 | pretooluse-design-doc-web-search-guard.sh | PreToolUse | Edit / Write / MultiEdit | stdin JSON に tool_name / tool_input が含まれる、PROJECT_ROOT が存在する | 対象外ツール/パス → pass(exit 0)。docs/adr/ADR-*.md が対象。変更規模が threshold(50行)未満 → pass。証跡(WebSearch/WebFetch/pmo-tech-fork/pmo-tech-docs または agent_slots) あり → pass。証跡なし → exit 2(block) | fail-close: 新規ファイル / 大規模変更(50行超) かつ証跡なし → block。docs/plans/ は対象外(pass)。HELIX_ALLOW_DESIGN_DOC_NO_WEB=1 + reason 必須で bypass。session_id 不明 + HELIX_DESIGN_DOC_GUARD_ALLOW_MISSING_SESSION=1 + reason で bypass 可 | あり (tests/test_design_doc_web_search_guard.bats が E2E カバー、test_helix_gate_design_doc_fail_close.py が Python ロジックカバー) | (1) ADR-*.md を新規作成（証跡なし）→ exit 2 (2) ADR-*.md を新規作成（WebSearch 証跡あり）→ pass (3) 変更量 <=50 行 → pass (4) docs/plans/ の PLAN-*.md → 対象外でpass (5) HELIX_ALLOW_DESIGN_DOC_NO_WEB=1 + reason → pass (6) HELIX_ALLOW_DESIGN_DOC_NO_WEB=1 かつ reason なし → block |
| FN-WSC-12 | pretooluse-opus-repo-block.sh | PreToolUse | Edit / Write / MultiEdit | stdin JSON に tool_name / tool_input.file_path が含まれる、helix_db.py が import 可能 | project_root が harness master と一致する場合: cli/** 以下のファイル → exit 2(block)。それ以外 → bypass(exit 0)。消費側 project の場合: project_root 配下の全ファイル → exit 2(block) | fail-close: 製造元は cli/** のみ block。bypass 条件: .helix/** パス、memory パス、HELIX_ALLOW_OPUS_REPO_EDIT=1+reason、HELIX_ALLOW_OPUS_PLAN_FIX=1+PLAN path、HELIX_SUPPRESS_HOOK=1。全操作前に audit_log へ hook_exec + gate_eval を記録する | あり (tests/harness-hooks.bats で一部カバー、test_merge_settings.py / test_context_guard.py で設定側カバー) | (1) 製造元 project の cli/helix-xxx を Edit → exit 2 (2) 製造元 project の docs/** を Edit → bypass(exit 0) (3) HELIX_ALLOW_OPUS_REPO_EDIT=1 + reason → pass (4) HELIX_ALLOW_OPUS_REPO_EDIT=1 かつ reason 未設定 → block (5) .helix/ パス → bypass (6) memory パス → bypass (7) HELIX_SUPPRESS_HOOK=1 → suppressed で exit 0 |
| FN-WSC-13 | sessionstart-harness-summary.sh | SessionStart | (SessionStart イベント) | cli/lib/agent_slots.py が import 可能、harness_monitor.py が import 可能 | stale slot 数 > 0 → リリース推奨メッセージを stdout に出力。直近24h の critical event > 0 → critical count メッセージを stdout に出力。それ以外 → 何も出力しない | fail-open: timeout 3 秒で失敗した場合は 0 扱い。例外は `2>/dev/null || echo 0` で握り潰し。メッセージ出力のみで system を変更しない | 部分あり (test_helix_agent_slots_release.py / test_harness_monitor_unit.py が list_stale_slots / list_recent_events をカバー。hook script 直接テストなし) | (1) stale slot あり → stdout に "[harness] 前 session の release 漏れ slot:" が出る (2) stale slot なし → 何も出力しない (3) critical event あり → "[harness] 直近 24h の critical event:" が出る (4) timeout / exception → 0 扱いで何も出力しない |
| FN-WSC-14 | sessionstart-history-injection.sh | SessionStart | (SessionStart イベント) | stdin JSON に source / session_type / transcript_path が含まれる、PROJECT_ROOT が存在する | session_type が "cleared" または "compacted" の場合のみ bundle を生成して additionalContext に注入する。handover の Next Action / PLAN 要約 / feedback memory を最大 1800 文字で trunc して返す。それ以外の session_type → 空文字で continue | fail-open: 全読み込み失敗は空文字 fallback。python3 不在は exit 0(hook skip)。bundle 生成失敗でも hookSpecificOutput を返す（空文字） | あり (test_session_start_helpers.py が extract_next_action 等の Python ロジックをカバー) | (1) session_type=cleared → bundle が生成されて additionalContext に含まれる (2) session_type=new → 空文字で additionalContext なし (3) handover CURRENT.md なし → handover 行なしで bundle が返る (4) bundle 文字数超過 → 1800 文字で trunc され末尾が "…" になる |
| FN-WSC-15 | stop-recovery-update.sh | Stop | (Stop イベント) | HELIX_PROJECT_ROOT/.helix/recovery/CURRENT.json が存在する、status が "active" である、cli/lib/recovery_workflow_engine.py が import 可能 | CURRENT.json の status=active の場合、recovery_workflow_engine.snapshot_on_stop() を呼び出してスナップショットを保存し、推奨メッセージを stdout に出力する | fail-open: CURRENT.json 不在 → exit 0 で何もしない。status が active 以外 → exit 0。snapshot_on_stop() 失敗は set -euo pipefail により異常終了するが、Claude Code の Stop hook は session を継続する設計 | あり (test_helix_recovery.py が snapshot_on_stop をカバー) | (1) CURRENT.json に status=active → snapshot_on_stop が呼ばれ stdout にメッセージが出る (2) CURRENT.json 不在 → exit 0 で無出力 (3) status=completed → exit 0 で無出力 (4) JSON parse 失敗 → status="" で exit 0 |
| FN-WSC-16 | stop.sh | Stop | (Stop イベント) | helix-common.sh がロード可能、helix_db.py が import 可能、stdin に session telemetry JSON が含まれる | hook_exec の audit ログを DB に insert する。session_telemetry を upsert する(session_id / tool_uses / tokens_total / cost_usd / ended=True) | fail-open: record_hook_audit は `>/dev/null 2>&1 || true`、record_session_telemetry は `|| true`。session_id 欠如は warn + exit 0 | あり (test_session_telemetry.py が upsert_session_telemetry をカバー、test_audit_log.py が insert_audit_log をカバー) | (1) 正常な stdin JSON → audit_log + session_telemetry が upsert される (2) session_id 欠如 → telemetry を warn+skip して exit 0 (3) tool_uses がリスト → len() で件数変換 (4) tokens_total が dict 形式 → "total" キーで再帰解析 (5) cost_usd が負数 → ValueError で telemetry skip |
| FN-WSC-17 | userpromptsubmit-context-bundle.sh | UserPromptSubmit | (UserPromptSubmit イベント) | stdin JSON に prompt フィールドが含まれる、PROJECT_ROOT が存在する | prompt に handover / carry / 継続 / resume / next action / PLAN-\d+ のいずれかを含む場合のみ bundle を生成して additionalContext に注入する。PLAN 要約 / handover Next Action / feedback memory を最大 1500 文字で trunc。keyword 非該当 → 空文字で返す | fail-open: 全読み込み失敗は空文字 fallback。python3 不在は exit 0。bundle が空でも hookSpecificOutput を返す | 部分あり (test_session_start_helpers.py が関連 helper をカバー、hook script 直接テストなし) | (1) prompt に "PLAN-001" → bundle が生成されて additionalContext に含まれる (2) prompt にキーワード非含む → 空文字で additionalContext なし (3) PLAN-*.md が存在しない → plan 行なしで bundle が返る (4) feedback_*.md が複数 → mtime 降順で最大2件が含まれる |

## §B. cli/lib batch A (FN-WSC-101〜119)

| FN-ID | module | 主関数 | requires | ensures | invariant | 既存テスト |
|---|---|---|---|---|---|---|
| FN-WSC-101 | `allowed_files_estimator.py` | `estimate_allowed_files(task, repo_root, threshold=0.5)` | `task` は str（空可）; `repo_root` は存在する Path | `tuple[list[Path], float, bool]` を返す。低信頼時は `([], confidence, True)`、高信頼時は `(candidates, confidence, False)` | `threshold` 未満 or candidates 空なら空リストで fail-close（絶対に None を返さない）; `repo_root` 配下の `.git` 等 `_IGNORED_DIRS` はスキャン除外 | あり (`test_allowed_files_estimator.py`) |
| FN-WSC-102 | `code_recommender.py` | `find_code(query, n=5, *, bucket="coverage_eligible")` | `query` は非空 str; `n >= 1`; `bucket` は `{"coverage_eligible","private_helper","excluded","all"}` のいずれか | `list[dict]` を返す（`id/domain/summary/path/line_no/score/reason` を含む）。DB エントリ無し時は空リスト | `query` 空→ `CodeRecommenderError(2)` raise; `bucket` 不正→ `CodeRecommenderError(2)` raise; LLM 障害時はローカル語彙フォールバックに切り替え（warn-only、例外は伝播しない）; JSON 解析失敗時はリトライ後に `CodeRecommenderError(4)` | あり (`test_code_recommender.py`) |
| FN-WSC-103 | `codex_post_validation.py` | `find_allowed_files_violations(*, before_paths, after_paths, untracked_after_paths, allowed_patterns, concurrent_baselines=None)` | `allowed_patterns` は `list[str]`; 各 paths は `set[str]`; `concurrent_baselines` は `list[set[str]]` または None | 許可パターン外の新規ファイル path を `list[str]` で返す（空リスト = 違反なし）| `allowed_patterns` 空→空リスト（実行せず即返す = fail-safe）; 並行 Codex session 由来の ambiguous_tracked パスは violations から除外（false-positive 抑制） | あり (`test_codex_post_validation.py`) |
| FN-WSC-104 | `demotion_checker.py` | `check_demotion_eligibility(rule_id, days, violation_history)` | `rule_id` は str; `days >= 0`; `violation_history` は iterable | `bool`: `days >= DEMOTION_WINDOW_DAYS(30)` かつウィンドウ内に `rule_id` 一致の違反が無い場合 `True` | `days < 30` → 即 `False`（閾値下では降格不可を保証）; エントリが Mapping でない場合でもクラッシュせず `True` を維持（型不一致は violation なしと扱う） | あり (`test_demotion_checker.py`) |
| FN-WSC-105 | `doc_map_matcher.py` | `main() → int` / `_emit_matches(triggers, file_path)` | `doc_map_path` はファイルとして存在する Path; `file_path` は str | `main` は 0/1/2 の int を返す; `_emit_matches` は `GATE_READY\|…`/`DESIGN_SYNC\|…`/`COVERAGE_CHECK\|…`/`ADR_INDEX\|…` を stdout に出力 | doc_map が存在しない場合 exit 0（fail-open）; parse 失敗時 exit 1; validate 警告は stderr に出力し処理継続; `on_write` が `ALLOWED_ACTIONS` 外のトリガーは無視（ガード済） | あり (`test_doc_map_matcher.py`) |
| FN-WSC-106 | `doctor_plan_checks.py` | `run_check_plan_drift(conn)` | `conn` は有効な `sqlite3.Connection`; `plan_generates`/`plan_registry` テーブルが存在することが望ましい | `list[dict]` を返す（各 dict に `plan_id/artifact_path/artifact_type/status/reason/days_stale`）| 必要テーブル不在→ `[{"status":"warning","reason":"missing_tables",…}]`（例外なし）; アーティファクト存在チェック結果を `plan_generates.exists_check` に書き戻す（副作用あり）; `is_reference:true` PLAN はスキップ | あり (`test_doctor_plan_checks.py`) |
| FN-WSC-107 | `doctor_recovery_check.py` | `run_check_recovery_freshness(max_age_days=30)` | `HELIX_PROJECT_ROOT` 環境変数または cwd が有効; `docs/plans/PLAN-*-recovery-*.md` が対象 | `list[dict]` を返す（`plan_id/path/kind/doc_status/last_updated/age_days/status/reason/max_age_days`）; 対象ファイルなし→ 空リスト | `docs/plans/` ディレクトリ不在→ 空リスト（例外なし）; `kind != "recovery"` のファイルはスキップ; `revised_at` 取得失敗時はファイル mtime を代替使用 | あり (`test_doctor_recovery_check.py`) |
| FN-WSC-108 | `drift_db_diff.py` | `main() → int` | CLI 引数 `--doc <markdown>` と `--db <sqlite>` が必須; ファイルは双方とも存在する | stdout に `RESULT\|ok` または `RESULT\|warn` + `WARN\|<kind>\|<message>` 行を出力; 0 を返す（正常完了時）| doc 不在→ `ERROR\|missing-doc\|…` 出力 + exit 2; DB 不在→ `ERROR\|missing-db\|…` + exit 2; SQLite エラー→ `ERROR\|sqlite\|…` + exit 3; カラム型が両方空の場合は型差分とみなさない | あり (`test_drift_db_diff.py`) |
| FN-WSC-109 | `dual_write_connection.py` | `_DualWriteConnection.execute(sql, params=())` | `legacy_conn`/`new_conn` は有効な `sqlite3.Connection`; `sql` は有効な SQL; params は適切な型 | `sqlite3.Cursor` を返す（legacy の結果）; `lastrowid` に legacy の rowid を反映 | legacy 失敗→ 例外をそのまま raise; new DB 失敗（BUSY 以外）→ warning ログ + new DB rollback で処理継続（legacy 結果を守る）; new DB BUSY→ 例外 raise（busy は継続不可）; `executemany`/`commit`/`rollback`/`close` で同じポリシーを維持 | あり (`test_dual_write_connection_unit.py`) |
| FN-WSC-110 | `dual_write_mismatch.py` | `check_dual_write_mismatch(legacy_conn, new_conn, table_name, *, sample_size=1000)` | `legacy_conn`/`new_conn` は有効な `sqlite3.Connection`; `table_name` は有効な識別子; `sample_size >= 1`; `detector_runs` テーブルが `new_conn` に存在する | `MismatchResult(detected, table_name, legacy_row_count, new_row_count, mismatch_keys, detected_at, severity)` を返す; 結果は `new_conn` の `detector_runs` に INSERT される | `sample_size <= 0`→ `ValueError`; テーブル名に無効文字→ `ValueError`（SQL インジェクション防止）; テーブル不在→ `severity="critical"`, `mismatch_keys=["__missing_table__"]`; PK スキーマ不一致→ `severity="critical"`; `detector_runs` 不在→ `RuntimeError` | あり (`test_dual_write_mismatch_unit.py`) |
| FN-WSC-111 | `effort_classifier.py` | `EffortClassifier.classify(task_text, role=None, size=None, files=None, lines=None, use_llm=True)` | `task_text` は str; `role` は str or None; `size` は `"S"/"L"` or None; `files/lines` は int or None | `dict` を返す（`effort/score/breakdown/reason/recommended_thinking/split_recommended/llm_used/cached`）; `effort` は `"low"/"medium"/"high"/"xhigh"` のいずれか | LLM 不可時はルールベースにフォールバック（`_llm_failed=True` でサイレント）; `effort` が 4 値以外の応答→ `CodexResponseError` raise; キャッシュ OSError は黙殺（warn-only）; score 境界値はルールで LLM に委譲 | あり (`test_effort_classifier.py`) |
| FN-WSC-112 | `freeze_checker.py` | `run(phase_file, index_file, file_path) → int` | `phase_file` は存在する YAML ファイル; `index_file` は JSON（存在しない場合は空 dict 扱い）; `file_path` は相対 or 絶対 path | 0 を返す（常に）; freeze-break 検出時は `[helix-hook] WARN:…` を stdout に出力; 無効化した gate を `phase_file` に書き戻す（副作用） | `phase_file` が空→ exit 0（fail-open）; `file_path` が空→ exit 0; layer が不明→ exit 0（false-negative より false-positive を避ける）; gate が passed でなければ freeze-break とみなさない | あり (`test_freeze_checker.py`) |
| FN-WSC-113 | `gate_check_generator.py` | `build_gate_checks(matrix, deliverables_rules, structure, framework=None, *, catalog_index, resolve_paths, d_contract_doc_pattern)` | `matrix["features"]` が dict; `catalog_index`/`resolve_paths`/`d_contract_doc_pattern` は callable; `deliverables_rules` と `structure` は dict | `dict[str, dict]` を返す（各 gate ID → `{name, static:list, ai:list}`; G 番号昇順にソート済み） | `matrix["features"]` が dict でない→ `ValueError` raise; deliverable/validator の型不正は黙殺（スキップ）; `build_doc_map` も同じ引数で `{triggers:list}` を返す; gate 名は `_gate_name` で正規化; 重複 check は seen set で除外 | あり (`test_gate_check_generator.py`) |
| FN-WSC-114 | `global_store.py` | `sync_from_local(local_db, project_id)` | `local_db` は存在する SQLite パス str; `project_id` は非空 str; `~/.helix/` が書き込み可能 | `dict` を返す（`project_id/synced_patterns/synced_runs/global_db`）; `global_db` は初期化済みの `~/.helix/global.db` パス | `local_db` 不在→ `ValueError` raise; `task_runs` の解析失敗 run は警告 stderr 出力でスキップ（全体失敗にしない）; secret キー（password/token 等）は `[REDACTED]` に置換してから global_path に書き込む; `HELIX_GLOBAL_HOME` env で `~/.helix` を上書き可能 | あり (`test_global_store.py`) |
| FN-WSC-115 | `job_p0_guard.py` | `validate_authorization(authorization_ref, source_plan_id, authorized_by, current_phase)` | `current_phase` は `{"P1","P2","P3"}` のいずれか（None → "P1"); `authorized_by` は CSV/JSON/pipe 区切りの文字列 or None | `(bool, list[str])` を返す; P1 フェーズは常に `(True, messages)` | `current_phase` が `VALID_PHASES` 外→ `ValueError` raise; `authorized_by` に `AUTHORIZATION_ALIASES` 外の値→ messages に追加するが raise しない; P3 で認可失敗→ exit code 2; P1/P2 で認可失敗→ exit code 1 | あり (`test_job_p0_guard.py`) |
| FN-WSC-116 | `llm_classifier_base.py` | `LLMClassifierBase.classify(query, context=None)` | `classifier_name` と `role` がサブクラスで設定済み; `query` は str; Codex CLI が利用可能（なければフォールバック可） | `dict` を返す（サブクラスの `_decorate_result` が加工した分類結果）; 結果は TTL キャッシュに保存; `entries` テーブルへ decision を INSERT（warn-only、失敗しても例外にしない） | `classifier_name` 未設定→ `ValueError` raise; `role` 未設定→ `ValueError` raise; Codex 呼び出し失敗→ `CodexInvocationError` raise（サブクラスでキャッチ可）; Codex 応答が JSON 非オブジェクト→ `CodexResponseError` raise; DB 記録失敗は logging.warning で黙殺 | あり (`test_llm_classifier_base.py`) |
| FN-WSC-117 | `phase_guard.py` | `main() → int` | `--phase-file` は存在する YAML; `--file` は path str; `--index` は JSON(省略可) | 0（allow）または 1（blocked）を返す | phase_file 不在→ 0（fail-open）; `_is_guard_exempt`（テストファイル/`.helix/`/ADR 等）→ 0; current_phase が空/NULL→ src 変更は 1、それ以外は 0; target_layer が不明/None→ 0; index.json 読み込み失敗は `_log_guard_error` で stderr 出力後 1; すべての例外は最外層で捕捉して 1 | あり (`test_phase_guard.py`) |
| FN-WSC-118 | `plan_lint.py` | `validate_plan_frontmatter(frontmatter: dict) → list[dict[str, str]]` | `frontmatter` は dict（YAML パース済み）; 呼び出し前に YAML parse エラーは捕捉済み | `list[dict]` を返す（各要素: `{level:"error"/"warning", field:str, message:str}`）; 問題なし→ 空リスト | `generates` が list でない→ `{"level":"warning"}` で追加（例外なし）; `kind/layer/drive/status` の enum 違反は `{"level":"error"}`; `process_layer` が `L0`〜`L14` 外→ error; V2 plan（ファイル名/frontmatter_plan_id がパターン一致）は自動で validate; PLAN 番号 < 36 はスキップ | あり (`test_plan_lint.py`) |
| FN-WSC-119 | `plan_parser.py` | `parse_frontmatter(filepath: str) → dict | None` | `filepath` は str; ファイルが `PLAN-`/`ADR-` で始まる `.md` であることが前提 | dict を返す（YAML パース済み frontmatter）; 非対象ファイル→ `{}`; parse 失敗→ `None` | ファイル読み込み OSError→ `None` + `_warn()`; frontmatter ブロック不在→ `None` + `_warn()`; YAML 不正→ `None` + `_warn()`; required fields 欠落→ `loaded["_warnings"]` に追記した dict を返す（None にしない）; `upsert_plan` の SQLite エラーは `_warn()` のみで exit 0 | あり (`test_plan_parser.py`) |

## §C. cli/lib batch B (FN-WSC-201〜218)

| FN-ID | module | 主関数 | requires | ensures | invariant / fail-close | 既存テスト |
|---|---|---|---|---|---|---|
| FN-WSC-201 | push_gate | `run_all_gates(execute, remote, branch, plan_id, allow_main)` | repo が git 管理下にある。`HELIX_PROJECT_ROOT` または `git rev-parse --show-toplevel` が成功する。 | 7 gate を順に実行し `{"ok": bool, "failed_count": int, "gates": [...], "push": {...}}` を返す。全 gate pass かつ `execute=True` かつ `branch != "main" or allow_main` の場合のみ `git push` を実行する。 | `execute=False` なら push しない。`branch=="main" and not allow_main` なら ok=False で返す（push しない）。gate 結果は常に 7 要素。hermetic env で HELIX_AUTOMATION_* を除去してテスト subprocess を起動する。 | あり (test_push_gate.py) |
| FN-WSC-202 | recovery_plan_check | `check_recovery_template_sections(filepath)` | `filepath` が存在し UTF-8 で読める markdown ファイルである。 | REQUIRED_TEMPLATE_SECTIONS の各セクション (7種) の有無を見出し行で検査し、不足セクション名の `list[str]` を返す。充足なら空リスト。 | ファイル読み取りエラーは呼び出し元に伝搬 (wrap しない)。セクション判定は小文字化見出し部分一致。 | あり (test_recovery_plan_check.py) |
| FN-WSC-203 | research_guard | `check_research_gate(root)` | `root` が有効なディレクトリ Path。 | `docs/research/**`, `.helix/research/**`, `D-RES/**` 以下のアクティブ (archive/generated でない) レポートを探索し、各ファイルが source_evidence・adoption_decision・open_blockers 未存在の欠陥を検査する。`{"ok": bool, "reports": [...], "errors": [...]}` を返す。 | レポートが 0 件なら `missing_research_report` error を必ず含む。`ok=True` は errors 空のとき限定。 | あり (test_research_guard.py) |
| FN-WSC-204 | research_tool_guard | `main(argv)` | stdin が JSON または空文字列。`tool_name` フィールドが存在する場合は文字列。 | `tool_name` が `ALLOWED_TOOLS = {"WebSearch","WebFetch"}` に含まれるか空の場合は exit 0。含まれない場合は stderr に DENY を出力し exit 1。stdin の JSON パースエラーまたは非 dict は exit 1。 | DENY 出力後は必ず exit 1 (fail-close)。空 stdin は通過 (exit 0)。 | あり (test_research_tool_guard.py) |
| FN-WSC-205 | skill_classifier | `classify_skill(skill_id, skill_md_content, *, known_task_ids, allowed_agents, allowed_phases, template_path, helix_codex_path)` | `skill_id` が非空文字列。`known_task_ids` は `set[str]`。`helix-codex` が起動可能。 | LLM (Codex) へプロンプトを送り JSON レスポンスを `_validate_classification` でバリデーションし `{"phases", "tasks", "triggers", "anti_triggers", "agent", "similar", "confidence"}` の dict を返す。ネットワーク/パースエラーは最大 3 回リトライ後に `ClassifierError` を raise。 | `confidence ∈ [0.0, 1.0]`。`agent ∈ ALLOWED_AGENTS`。`phases ⊆ ALLOWED_PHASES`。Codex exit code が NETWORK_EXIT_CODES の場合は `ClassifierError(7)` を raise。 | あり (test_skill_classifier.py) |
| FN-WSC-206 | skill_frontmatter_lint | `validate_skill_frontmatter(frontmatter)` | `frontmatter` が `dict` (SKILL.md の YAML frontmatter 解析済み)。 | `name`/`description`/`triggers` の必須フィールド、`description` の長さ (≥50文字)、`metadata.helix_layer` の有効値 (L1-L14 または all) を検査し `list[dict[str, str]]` (level/field/message) を返す。問題なしは空リスト。 | `name`/`description` 欠落は error、`skill_id` 欠落は warning、`description` 短すぎは warning。helix_layer が不正値なら error。 | あり (test_skill_frontmatter_lint.py) |
| FN-WSC-207 | skill_recommender | `recommend(task_text, top_n, layer_filter, category_filter, catalog_path, cache_dir, jsonl_catalog_path, phase_filter, use_no_jsonl, force_refresh)` | `task_text` が非空文字列。`top_n >= 1`。catalog が読み取り可能か自動ビルドできる。 | スキルカタログ (JSON/JSONL) を読み込み Codex を呼び出して `{"candidates": [...], "task_summary": str, "no_match_reason": str|None, "_cached": bool, "_model": str}` を返す。JSONL に approved エントリがある場合は JSONL モードで Codex を呼ぶ。 | `task_text` が空なら `RecommenderError(2)` を raise。`candidates` は `top_n` 件以下。各 candidate の `score ∈ [0.0, 1.0]`。キャッシュ TTL 超過エントリは GC する。 | あり (test_skill_recommender.py) |
| FN-WSC-208 | skip_annotation_linter | `lint_skip_annotations(paths, *, strict, today)` | `paths` が iterable の file/dir パス列。Python ファイルが UTF-8 で読める。 | 各 `.py` ファイルを AST 解析し pytest/unittest skip 呼び出しを抽出。`HELIX-SKIP:` プレフィックスなし → UNSTRUCTURED_SKIP warning、フォーマット不正 → INVALID_SKIP_ANNOTATION error、期限超過 (≥30日) → OVERDUE_SKIP_ANNOTATION (strict=True なら error、else warning) を検出し `LintResult` を返す。 | SyntaxError ファイルは SYNTAX_ERROR finding として記録し処理を続ける。OSError ファイルは UNREADABLE_FILE finding。`exit_code == 0` は findings が空のとき限定。 | あり (test_skip_annotation_linter.py) |
| FN-WSC-209 | sprint_auto_check | `auto_check(sprint_id, target_paths)` | `sprint_id` が非空文字列。`target_paths` は str のリスト。`sprint_lint` がインポート可能。 | `run_py_compile(target_paths)` → `run_pytest(target_paths)` → `run_full_suite()` を順に実行し `{"sprint_id", "step4_py_compile", "step5_pytest", "step5_full", "overall_pass": bool}` を返す。 | `overall_pass` は step4 pass かつ pytest failed==0 かつ full status が pass/skip/skipped の場合のみ True。target_paths 空なら pytest に errors=["no target paths"] を返す。 | あり (test_sprint_auto_check.py) |
| FN-WSC-210 | sprint_lint | `audit_sprint_completion(plan_id, sprint, *, skip_full_regression)` | `plan_id` が非空文字列。`sprint` が非空文字列。PROJECT_ROOT が解決できる。 | git status から変更ファイルを収集し py_compile/relevant_tests/full_regression/review の 4 check を実行し `{"plan_id", "sprint", "checks", "overall": "pass"|"warn"|"fail", "target_files", "relevant_test_selector"}` を返す。 | `overall="fail"` は py_compile・relevant_tests・full_regression のいずれかが fail のとき。`overall="warn"` は review missing または full_regression skipped のとき。 | あり (test_sprint_lint.py) |
| FN-WSC-211 | task_type_inference | `detect_task_type(task)` | `task` が文字列 (空文字列を許容)。 | `[タスク種別]` マーカーが明示されていれば既知値 (実装/レビュー/設計/調査) を抽出、不明マーカーは "不明"。マーカーなしは正規表現パターンの最初マッチ位置でタスク種別を推定し "不明" を返す。 | 戻り値は `{"実装","レビュー","設計","調査","不明"}` のいずれか。空文字列入力では "不明" を返す。 | あり (test_task_type_inference.py) |
| FN-WSC-212 | trace_symmetry | `collect_trace_symmetry(project_root)` | `project_root` が有効なディレクトリ。`docs/v2/` に markdown ファイルが存在する (なくても空 dict で返る)。 | 5 V-model pair (L1↔L14/L3↔L12/L4↔L9/L5↔L8/L6↔L7) について design 側エントリの coverage_pct / orphan_test / missing_pair / wrong_layer_pair / balance_ratio を計算し `{"advisory": True, "exit_code": 0, "pairs": {...}, "preflight_fail": {...}}` を返す。 | `advisory=True` かつ `exit_code=0` 固定 (警告のみ、終了コードを上げない)。design/test doc が存在しない pair は coverage_pct=0.0, balance_ratio=0.0 として計上する。 | あり (test_trace_symmetry.py) |
| FN-WSC-213 | uuid7_generator | `generate_event_id()` | Python 3.12 以上。`os.urandom` が使用可能。 | RFC 9562 準拠 UUID v7 文字列 (36文字、"xxxxxxxx-xxxx-7xxx-[89ab]xxx-xxxxxxxxxxxx" 形式) を返す。Python 3.14+ で `uuid.uuid7` があれば stdlib を使用、なければ fallback で生成する。 | 戻り値の `[2][0] == "7"` かつ `[3][0] ∈ {"8","9","a","b"}`。`os.urandom` 失敗時は `RuntimeError` を raise。同一 ms 内連続呼び出しでも strict monotonic を保証 (counter increment)。 | なし |
| FN-WSC-214 | vmodel_lint | `lint_plan(plan_path, grandfather, project_root)` | `plan_path` が存在する PLAN markdown ファイル。`grandfather` が `set[str]`。 | PLAN テキストから design/impl/test_design/test_code の 4 種参照数を正規表現でカウントし `LintResult` を返す。参照数 0 のカテゴリを `missing` リストに追加する。`is_draft=True` の場合は missing を評価しない。 | `LintResult.complete` は 4 参照数すべて > 0 のとき True。grandfather PLAN は complete 判定を skip する。plan_id は frontmatter `plan_id:` → ファイル名 → stem の優先順で抽出する。 | あり (test_vmodel_lint.py) |
| FN-WSC-215 | vmodel_pair_freeze | `check_pair_freeze(layer, *, project_root, active_only, since_days)` | `layer` が L0-L14 の文字列。`project_root` が有効な Path または None。 | `docs/plans/{pair}/` 下の `{pair}-*plan.md` を探索し pair doc 存在/staleness/status 分布を返す `{"layer", "pair", "severity", "pair_doc_exists": bool, "status": "ok"|"pair_missing"|"no_pair", ...}` を返す。 | L0/L11/L13 など対のない layer は `"no_pair"` status で返す。`pair_doc_exists=False` なら hint にディレクトリを案内する。stale_count は `since_days` 指定時のみカウントする。 | あり (test_vmodel_pair_freeze.py) |
| FN-WSC-216 | workflow_dsl_parser | `validate_workflow_schema(payload)` | `payload` が dict (load_workflow で読み込んだ YAML)。 | `workflow_id` キーがあれば workflow DSL、`harness_id` キーがあれば harness DSL としてバリデーションし、違反メッセージの `list[str]` を返す。問題なしは空リスト。 | `role` は ALLOWED_AGENT_ROLES に含まれる必要がある。step/check の `id` は一意。`escalation_level ∈ {"L0","L1","L2","L3"}`。harness の `on_fail ∈ {"reject","review_inject","escalate","warn"}`。`workflow_id` も `harness_id` もない場合は error を返す。 | あり (test_workflow_dsl_parser.py) |
| FN-WSC-217 | yaml_parser | `write_yaml_safe(filepath, dotpath, value)` | `filepath` が有効な YAML ファイルパス。`dotpath` がドット区切りのキーパス。排他ロック取得が可能。 | fcntl + file_lock で排他ロックを取得し、YAML を parse → dotpath にキーをセット → dump → atomic rename (tmp → filepath) で安全に上書きする。ヘッダーコメントを保持する。 | `os.replace` で atomic 書き込みを保証する。ロック解放は finally で実行する。同一 ms 内の並行書き込みは順次実行される (LOCK_EX)。PyYAML 未使用 (stdlib のみ)。 | あり (test_yaml_parser.py) |
| FN-WSC-218 | zizmor_ignore_lint | `scan_paths(paths)` | `paths` が `list[Path]` (ファイルまたはディレクトリ)。ディレクトリは `**/*.yml` / `**/*.yaml` を再帰スキャン。 | 各ファイルの `# zizmor:ignore[...]` コメントを正規表現で検出し、reason/owner/expires (または re-evaluate-when) の metadata 有無を検査して `list[IgnoreFinding]` を返す。 | 読み取り不可ファイルは空リストで skip (エラーを raise しない)。`IgnoreFinding.is_compliant` は 3 フィールド全て有りのとき True のみ。 | なし |
| FN-WSC-219 | g7_subcheck | `collect_g7_subcheck(root, execute_tests)` | `root` が有効な repo Path。`docs/v2/L7-test-design/g7-test-anchor-map.yaml` が load 可。 | canonical UT inventory に対し anchor 済み / test_execution_pass を集計し `anchored / ut_total / exec_pass / missing / unanchored_but_exists` を持つ report を返す。`execute_tests=False`（または `HELIX_DOCTOR_SKIP_EXEC_TESTS`）で exec を抑制可。 | 結果は決定的。missing/unanchored を部分黙殺しない。advisory（exit_code=0）であり push block は VG-overview 経由。 | あり (test_g7_subcheck.py) |
| FN-WSC-220 | vg_overview | `collect_vg_overview(root, execute_g7_tests, strict_full_flow)` | `root` が有効な repo Path。registry / anchor map / 各 detector が呼出可。 | `required_clean`(registry_design_coverage / fn_ut_pair_coverage / design_id_existence / ddd_bc_coverage / source_scan_vs_registry / registry_trace_complete / requirement_drift) と `pair_status`(L1-L14..L6-L7) を集約し `overall_clean = all(required_clean) ∧ applicable pair clean ∧ full_flow(strict時)` を返す。 | required_clean の 1 項目でも false → overall_clean=false（G-vg-overview で fail-close）。applicable / approved_deferred / not_applicable を区別。 | あり (test_vg_overview.py) |
| FN-WSC-221 | fn_ut_pair_coverage_checks | `check_fn_ut_pair_coverage(registry_path, anchor_map_path, root)` | registry / anchor map が load 可。 | L6_required の design_ids(FN)↔test_design_ids(UT) を anchor map と突合し `DetectorReport`(mode=advisory) を返す。 | `missing_test_design`(FN に対応 UT 無し) / `unanchored_ut`(UT が anchor map 不在、RD-UT-* 除外) / `orphan_ut`(anchored UT に対応 FN 無し) / `duplicate_test_design` を検出。`fn_ut_pair_waivers`(approved_deferred) は抑制。部分黙殺しない。 | あり (test_fn_ut_pair_coverage_checks.py) |
| FN-WSC-222 | design_id_existence_checks | `check_design_id_existence(registry_path, design_doc_glob, root)` | registry が load 可。L6 設計 doc glob が解決可。 | L6_required の design_ids(FN) が `docs/v2/L6-functional-design/*.md` に section/ID 言及として実在するか検査し `DetectorReport`(mode=advisory) を返す。 | `missing_design_section`(どの L6 doc にも FN-ID 不在) を検出。prefix 整合(必要条件)を実 doc 出現まで強化。`design_id_existence_waivers`(approved_deferred) は抑制。 | あり (test_design_id_existence_checks.py) |
| FN-WSC-223 | l7_worklist | `collect_l7_worklist(registry_path, anchor_map_path, root)` | registry / anchor map が load 可。 | L6_required の各 FN を `anchored` / `waived` / `separate_inventory`(RD-UT-*) / `missing_ut` に分類し worklist + summary(total/anchored/waived/separate_inventory/missing) を返す（registry 由来 L7 工程表 view）。 | **read-only（fail-close にしない）= 工程表ビュー**。RD-UT-* は `separate_inventory` 扱いで `missing` に数えない。registry を SSoT とし派生 view のみ生成（DB 拡張・PLAN 自動起票なし）。 | あり (test_l7_worklist.py) |

## §D. 合格基準（G6 → L7）

この章は whole-source coverage の L6↔L7 ペア条件を記録するものであり、現在の L1-L6 監査で新規 L7 作業を許可するものではない。新規 L7 実装、単体テスト設計成果物、単体テスト実施、coverage closure へ進む場合は、別途承認済み add-feature / PLAN を入口にする。

- 54 FN-WSC-* が L7 UT-WSC-* と 1:1（trace_symmetry L6↔L7 balance≥1.0 / coverage100% / orphan0 / missing0 維持）。
- 各 FN の invariant（特に hook の fail-close/fail-open 方向、lib guard の fail-close）が UT で反証されること。
- 実装は既存 module（実体あり=Reverse 由来）。テスト未整備 module は L7 §未整備一覧で carry（FN/UT 設計は完備）。
